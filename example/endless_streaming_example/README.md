# Endless streaming example

Demonstrates the package's experimental endless-streaming service with microphone audio from `flutter_sound_lite`.

From this directory, run `flutter pub get` and `flutter run`. Configure microphone permissions and valid Google Cloud credentials locally. Endless streaming is experimental and should be validated for your production workload.

See the package [README](../../README.md) for complete setup and security guidance.
