# Flutter Sound streaming example (V1)

Demonstrates live microphone transcription with `flutter_sound_lite` and the Google Cloud Speech-to-Text V1 API.

From this directory, run `flutter pub get` and `flutter run`. Configure microphone permissions and valid credentials locally. Never commit service-account credentials.

See the package [README](../../README.md) for complete setup and security guidance.
