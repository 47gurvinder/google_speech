# Microphone streaming example

Demonstrates live recognition using microphone audio supplied by `sound_stream`.

From this directory, run `flutter pub get` and `flutter run`. Configure microphone permissions and valid Google Cloud credentials locally. Never commit service-account credentials.

See the package [README](../../README.md) for complete setup and security guidance.
